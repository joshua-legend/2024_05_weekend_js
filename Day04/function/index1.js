//화살표 함수
const plus100_ = (x) => x + 100;
plus100(200); // 300
//2. 원소 3번째 것을 돌려주기
const getThrid = (x) => x[2];
getThrid(["a", "b", "c"]);
//3. 소문자화 시키고 😎붙히기
const lowerEmoji = (x) => x.toLowerCase() + "😎";
lowerEmoji("APPLE");
